import socket

#(i) endereço IP de origem, (ii) endereço IP de destino, (iii) TTL e(iv) TOS.
    
class Dados:
    def _init_(self, orig, dest, TTL, TOS):
        self.host
        self.port
        self.orig = ori
        self.dest = dest
        self.TTL = TTL
        selft.TOS = TOS


